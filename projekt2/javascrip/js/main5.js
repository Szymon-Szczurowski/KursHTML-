const colors = ['red', 'green', 'blue']

//odwołanie się do całej talbicy
console.log(colors);

//odwołanie się do konketnego elementu 
console.log(colors[2]);

//dodaje nowy kolor do tablicy
colors.push('gold')

console.log(colors);